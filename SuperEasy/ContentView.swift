//
//  ContentView.swift
//  SuperEasy
//
//  Created by 春山恒誠 on 2026/04/26.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!. But, what was changed from the former release?")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
